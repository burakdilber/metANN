% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met_mlp.R
\name{met_mlp}
\alias{met_mlp}
\title{Train a Feed-Forward Multilayer Perceptron}
\usage{
met_mlp(
  formula = NULL,
  data = NULL,
  x = NULL,
  y = NULL,
  architecture = NULL,
  hidden_layers = NULL,
  activation = "relu",
  output_activation = NULL,
  task = c("auto", "regression", "classification"),
  optimizer = optimizer_pso(),
  loss = NULL,
  metrics = NULL,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{formula}{Optional model formula.}

\item{data}{Optional data frame used with \code{formula}.}

\item{x}{Optional numeric input matrix or data frame.}

\item{y}{Optional response vector.}

\item{architecture}{Optional MLP architecture object.}

\item{hidden_layers}{Integer vector giving the number of units in each
hidden layer.}

\item{activation}{Activation function for hidden layers.}

\item{output_activation}{Optional output activation function. If \code{NULL}, it
is selected automatically based on the task.}

\item{task}{One of \code{"auto"}, \code{"regression"}, or \code{"classification"}.}

\item{optimizer}{Optimizer object.}

\item{loss}{Optional loss function. If \code{NULL}, it is selected automatically
based on the task.}

\item{metrics}{Optional performance metrics. If \code{NULL}, default metrics are
selected automatically based on the task.}

\item{seed}{Optional random seed.}

\item{verbose}{Logical. If \code{TRUE}, progress information is printed.}
}
\value{
An object of class \code{"metann"}.
}
\description{
Convenience wrapper around \code{metann()} for training feed-forward multilayer
perceptrons.
}
\examples{
fit <- met_mlp(
  formula = Petal.Width ~ Sepal.Length + Sepal.Width + Petal.Length,
  data = iris,
  hidden_layers = c(5),
  optimizer = optimizer_pso(pop_size = 10, max_iter = 10),
  seed = 123,
  verbose = FALSE
)

fit
}
\references{
Montana, D. J., and Davis, L. (1989). Training Feedforward Neural Networks
Using Genetic Algorithms. Proceedings of the 11th International Joint
Conference on Artificial Intelligence, 762--767.

Ilonen, J., Kamarainen, J.-K., and Lampinen, J. (2003). Differential
Evolution Training Algorithm for Feed-Forward Neural Networks.
Neural Processing Letters, 17, 93--105.
doi:10.1023/A:1022995128597

Karaboga, D., and Ozturk, C. (2009). Neural Networks Training by
Artificial Bee Colony Algorithm on Pattern Classification.
Neural Network World, 19(3), 279--292.

Mirjalili, S. (2015). How Effective is the Grey Wolf Optimizer in Training
Multi-Layer Perceptrons. Applied Intelligence, 43, 150--161.
doi:10.1007/s10489-014-0645-7

Dilber, B., and Ozdemir, A. F. (2026). A novel approach to training
feed-forward multi-layer perceptrons with recently proposed secretary bird
optimization algorithm. Neural Computing and Applications, 38(5).
doi:10.1007/s00521-026-11874-x
}
