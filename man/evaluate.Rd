% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{evaluate}
\alias{evaluate}
\title{Evaluate a metANN Model}
\usage{
evaluate(object, newdata, y_true = NULL, metrics = NULL, threshold = 0.5, ...)
}
\arguments{
\item{object}{A fitted object of class \code{"metann"}.}

\item{newdata}{New data used for evaluation. For formula-based models, this
should be a data frame containing the response variable. For x-y models,
this should be a numeric matrix or numeric data frame.}

\item{y_true}{Optional true response values. Required for x-y models. For
formula-based models, if \code{NULL}, the response is extracted from \code{newdata}.}

\item{metrics}{Optional performance metrics. If \code{NULL}, the metrics stored
in the fitted model are used.}

\item{threshold}{Classification threshold for binary classification.}

\item{...}{Additional arguments passed to \code{predict()}.}
}
\value{
An object of class \code{"metann_evaluation"}.
}
\description{
Evaluates a fitted metANN model on new data.
}
\examples{
fit <- met_mlp(
  formula = Petal.Width ~ Sepal.Length + Sepal.Width + Petal.Length,
  data = iris,
  hidden_layers = c(5),
  optimizer = optimizer_pso(pop_size = 10, max_iter = 10),
  seed = 123,
  verbose = FALSE
)

evaluate(fit, newdata = iris)
}
